import React from 'react'; import { View, Text, StyleSheet, SafeAreaView, TouchableOpacity, ScrollView } from 'react-native'; import { useNavigation } from '@react-navigation/native'; import { Ionicons } from '@expo/vector-icons'; import { useTheme } from '../contexts/ThemeContext'; const NotificationsManagementScreen = () => { const navigation = useNavigation(); const { colors } = useTheme(); return (<SafeAreaView style={[styles.container, { backgroundColor: colors.background }]}> <View style={styles.header}> <TouchableOpacity onPress={() => navigation.goBack()} style={styles.backButton}> <Ionicons name='arrow-back' size={24} color={colors.text} /> </TouchableOpacity> <Text style={[styles.headerTitle, { color: colors.text }]}>NotificationsManagement</Text> <View style={styles.placeholder} /> </View> <ScrollView style={styles.content}> <Text style={[styles.comingSoon, { color: colors.text }]}>🚧 NotificationsManagement - En cours de développement</Text> </ScrollView> </SafeAreaView>); }; const styles = StyleSheet.create({ container: { flex: 1 }, header: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', paddingHorizontal: 16, paddingTop: 50, paddingBottom: 10 }, backButton: { padding: 8 }, headerTitle: { fontSize: 20, fontWeight: 'bold', flex: 1, textAlign: 'center' }, placeholder: { width: 40 }, content: { flex: 1, padding: 20 }, comingSoon: { fontSize: 24, fontWeight: 'bold', textAlign: 'center', marginTop: 50 } }); export default NotificationsManagementScreen;
